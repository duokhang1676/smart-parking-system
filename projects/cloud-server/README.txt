-file app.py: là app flask, chạy file này trước khi thực thi api
-file db.py: là file dùng để kết nối đến mongodb database
-routes: chứa các file quản lý api của từng collection

- Nhớ import database
-p/s: Tui mới test mấy cái api cơ bản với mấy cái bạn cần. Chưa viết hết api.